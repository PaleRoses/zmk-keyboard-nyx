# Left half (central)
if SHIELD_NYX_LEFT

config ZMK_KEYBOARD_NAME
    string
    default "Nyx"

endif

# Right half (peripheral)
if SHIELD_NYX_RIGHT

config ZMK_KEYBOARD_NAME
    string
    default "Nyx"

endif